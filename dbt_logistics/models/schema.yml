version: 2

models:
  - name: courier_performance
    description: "Final analytics table for courier risks"
    columns:
      - name: courier_id
        tests:
          - unique
          - not_null
      - name: risk_level
        tests:
          - accepted_values:
              values: ['Safe', 'Moderate', 'High Risk', 'Extreme Risk']